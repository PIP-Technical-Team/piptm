% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_shares.R
\name{.shares_target}
\alias{.shares_target}
\title{Compute target-variable shares per subgroup}
\usage{
.shares_target(dt, by = NULL, target_variable, grp = NULL)
}
\arguments{
\item{dt}{A \code{\link[data.table:data.table]{data.table::data.table()}} for a single survey.}

\item{by}{Character vector of grouping column names, or \code{NULL}.}

\item{target_variable}{Character name of the binary (0/1) column in \code{dt}.}

\item{grp}{Optional pre-computed \code{\link[collapse:GRP]{collapse::GRP()}} matching \code{by}.}
}
\value{
A \code{\link[data.table:data.table]{data.table::data.table()}} in long format with columns:
\describe{
\item{\verb{[by cols]}}{Grouping columns (present only when \code{by} is provided).}
\item{\code{population}}{Weighted population of the subgroup (all values,
not just target-positive).}
\item{\code{measure}}{One of \code{"target_within_group_share"} or
\code{"target_survey_share"}.}
\item{\code{value}}{The computed share (0–1); \code{NA} if the relevant denominator
is zero.}
}
}
\description{
Assumes \code{target_variable} is binary (0/1, validated upstream). For each
cell defined by \code{by}, computes two shares based on the weighted population
with \code{target_variable == 1}:
}
\details{
\itemize{
\item \code{target_within_group_share}:
\verb{weighted_pop(target == 1 in subgroup) / weighted_pop(subgroup)}
\item \code{target_survey_share}:
\verb{weighted_pop(target == 1 in subgroup) / weighted_pop(survey)}
}

When \code{by = NULL} both measures are identical by definition, since the only
group is the whole survey.
}
\keyword{internal}
