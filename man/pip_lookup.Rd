% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_maker.R
\name{pip_lookup}
\alias{pip_lookup}
\title{Resolve survey triplets to pip_ids via the manifest}
\usage{
pip_lookup(country_code, year, welfare_type, release = NULL)
}
\arguments{
\item{country_code}{Character vector of ISO3 country codes
(e.g. \code{c("COL", "BOL")}).}

\item{year}{Integer vector of survey years (same length as \code{country_code}).}

\item{welfare_type}{Character vector of welfare types (\code{"INC"} or \code{"CON"},
same length as \code{country_code}).}

\item{release}{Character scalar release ID (e.g. \code{"20260206"}). Defaults
to the current release as returned by \code{\link[=piptm_current_release]{piptm_current_release()}}.}
}
\value{
Character vector of matching \code{pip_id} strings (one per matched
triplet, in the order returned by the manifest join).  May be shorter
than the input if some triplets are unmatched.
}
\description{
Translates human-friendly survey identifiers (country code, year, welfare
type) into the canonical \code{pip_id} strings stored in the manifest.  This is
the recommended entry point for ad-hoc R usage when the full \code{pip_id} string
is not known.
}
\details{
The function performs an inner join against the manifest for the specified
release.  Unmatched triplets emit a \code{\link[cli:cli_abort]{cli::cli_warn()}} listing the
unresolved combinations but do not stop execution.
}
\examples{
\dontrun{
set_manifest_dir("//server/manifests")
ids <- pip_lookup(
  country_code = c("COL", "BOL"),
  year         = c(2010L, 2000L),
  welfare_type = c("INC", "INC")
)
}
}
\seealso{
Other api: 
\code{\link{table_maker}()}
}
\concept{api}
