% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_parquet.R
\name{validate_partition_consistency}
\alias{validate_partition_consistency}
\title{Validate schema consistency across all Parquet files in a partition directory}
\usage{
validate_partition_consistency(partition_dir)
}
\arguments{
\item{partition_dir}{Absolute path to a partition directory, e.g.
\verb{.../country_code=BOL/surveyid_year=2012/welfare_type=INC/}.}
}
\value{
A named list with \code{valid}, \code{errors}, \code{warnings}, \code{file} (set to
\code{partition_dir}) and an extra element:
\describe{
\item{\code{files_checked}}{Character vector of \code{.parquet} file paths found
in \code{partition_dir}.}
}
}
\description{
Reads the \strong{schema only} (not row data) of every \code{.parquet} file in
\code{partition_dir} and checks:
\enumerate{
\item At least one \code{.parquet} file exists.
\item All files share an identical schema (no schema drift between survey
versions stored in the same partition).
\item The partition key values embedded in each file's data match the
directory path.
\item No duplicate \code{survey_id} values across files in the partition.
}
}
\examples{
\dontrun{
res <- validate_partition_consistency(
  "path/to/arrow/country_code=BOL/surveyid_year=2012/welfare_type=INC/version=v01_v04"
)
res$valid
res$files_checked
}
}
\seealso{
Other parquet-validation: 
\code{\link{validate_parquet_data}()},
\code{\link{validate_parquet_schema}()}
}
\concept{parquet-validation}
