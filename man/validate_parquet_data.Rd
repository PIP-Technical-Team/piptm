% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_parquet.R
\name{validate_parquet_data}
\alias{validate_parquet_data}
\title{Validate the row data of a single Parquet file}
\usage{
validate_parquet_data(file_path)
}
\arguments{
\item{file_path}{Absolute path to a \code{.parquet} file.}
}
\value{
A named list with \code{valid}, \code{errors}, \code{warnings}, \code{file}.
See \code{\link[=validate_parquet_schema]{validate_parquet_schema()}} for the full structure.
}
\description{
Reads the \strong{full row data} and checks data-quality constraints defined in
\code{inst/schema/arrow-schema.json}. More expensive than
\code{validate_parquet_schema()} — do not call in hot paths.
}
\details{
Checks performed:
\enumerate{
\item \code{welfare} — all finite, all ≥ 0; warn if any == 0.
\item \code{weight} — all finite, all > 0, no NA.
\item \code{welfare_type} — all values in \verb{\{"INC", "CON"\}}.
\item \code{country_code} — matches ISO3 pattern; matches partition directory.
\item \code{surveyid_year} — matches \verb{year=} component of partition directory.
\item \code{welfare_type} data value — matches \verb{welfare=} component of partition
directory.
\item Partition key consistency — one unique value per key per file.
\item Factor level conformance for \code{gender}, \code{area}, \code{education} (if present).
\item \code{age} range \link{0, 130} (if present).
}
}
\examples{
\dontrun{
res <- validate_parquet_data("path/to/BOL_2012_EH_...-0.parquet")
res$valid
res$warnings  # e.g. zero-welfare rows
}
}
\seealso{
Other parquet-validation: 
\code{\link{validate_parquet_schema}()},
\code{\link{validate_partition_consistency}()}
}
\concept{parquet-validation}
