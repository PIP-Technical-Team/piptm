% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_parquet.R
\name{validate_arrow}
\alias{validate_arrow}
\title{Batch-validate Parquet files in the Arrow repository}
\usage{
validate_arrow(
  country_code = NULL,
  pip_ids = NULL,
  surveys = NULL,
  check = c("schema", "data"),
  arrow_root = piptm_arrow_root()
)
}
\arguments{
\item{country_code}{Character vector of ISO3 country codes to validate.}

\item{pip_ids}{Character vector of \code{pip_id} values to validate.}

\item{surveys}{A \code{data.table} / \code{data.frame} with a \code{pip_id} column.}

\item{check}{Character vector passed to \code{\link[=validate_parquet]{validate_parquet()}}.
Defaults to \code{c("schema", "data")}.}

\item{arrow_root}{Path to the Arrow repository root. Defaults to
\code{\link[=piptm_arrow_root]{piptm_arrow_root()}}.}
}
\value{
A \code{data.table} with one row per \code{.parquet} file and columns:
\describe{
\item{\code{pip_id}}{Survey identifier derived from the filename.}
\item{\code{file}}{Absolute path to the \code{.parquet} file.}
\item{\code{valid}}{\code{TRUE} / \code{FALSE}.}
\item{\code{n_errors}}{Number of hard errors.}
\item{\code{n_warnings}}{Number of soft warnings.}
\item{\code{errors}}{Errors collapsed with \code{" | "}, or \code{""} if none.}
\item{\code{warnings}}{Warnings collapsed with \code{" | "}, or \code{""} if none.}
}
}
\description{
Discovers \code{.parquet} files under \code{\link[=piptm_arrow_root]{piptm_arrow_root()}} and runs
\code{\link[=validate_parquet]{validate_parquet()}} on each one, returning a tidy \code{data.table} summary
(one row per file).
}
\details{
\strong{Filtering} — narrow the scope with one of:
\itemize{
\item \code{country_code}: one or more ISO3 codes (e.g. \code{"KAZ"} or \code{c("KAZ","COL")}).
\item \code{pip_ids}: one or more \code{pip_id} strings that determine the file names to
look for.
\item \code{surveys}: a \code{data.table} / \code{data.frame} with at least a \code{pip_id} column
(e.g. the output of \code{\link[=piptm_manifest]{piptm_manifest()}}).
}

Only one filter may be supplied at a time. If none is supplied the function
validates the entire repository (potentially slow).
}
\examples{
\dontrun{
# All KAZ files — schema + data checks
validate_arrow("KAZ")

# Several countries
validate_arrow(c("KAZ", "COL"))

# From a pip_inv subset (has a pip_id column)
validate_arrow(surveys = pip_inv_subset)

# Schema check only — much faster
validate_arrow("KAZ", check = "schema")

# Show only failures
res <- validate_arrow("KAZ")
res[valid == FALSE]
}
}
\seealso{
Other parquet-validation: 
\code{\link{validate_parquet}()},
\code{\link{validate_parquet_data}()},
\code{\link{validate_parquet_schema}()},
\code{\link{validate_partition_consistency}()}
}
\concept{parquet-validation}
