% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_shares.R
\name{.shares_setup}
\alias{.shares_setup}
\title{Prepare common inputs for shares computation}
\usage{
.shares_setup(dt, by = NULL, extra_cols = NULL, grp = NULL)
}
\arguments{
\item{dt}{A \code{\link[data.table:data.table]{data.table::data.table()}} for a single survey.}

\item{by}{Character vector of grouping column names, or \code{NULL}.}

\item{extra_cols}{Character vector of additional required column names
(e.g. the target variable name). Checked alongside \code{"weight"}.}

\item{grp}{Optional pre-computed \code{\link[collapse:GRP]{collapse::GRP()}} matching \code{by}.}
}
\value{
A named list with elements:
\describe{
\item{\code{w}}{Numeric weight vector.}
\item{\code{denom_survey}}{Numeric denominator aligned to each output cell.
For batched data grouped by \code{pip_id}, this is the weighted survey total
of the corresponding \code{pip_id}; otherwise it is the global total.}
\item{\code{grp}}{A \code{\link[collapse:GRP]{collapse::GRP()}} object, or \code{NULL} when \code{by = NULL}.}
\item{\code{cell_wpop}}{Numeric vector of per-cell weighted populations
(length = number of groups), or scalar survey total when \code{by = NULL}.}
\item{\code{groups_dt}}{A \code{\link[data.table:data.table]{data.table::data.table()}} of group keys with a
\code{population} column, or a one-row table with only \code{population} when
\code{by = NULL}.}
}
}
\description{
Validates required columns, extracts the weight vector, computes the survey
total, and builds the grouping object. Called by \code{.shares_population()} and
\code{.shares_target()} before any measure-specific logic.
}
\keyword{internal}
