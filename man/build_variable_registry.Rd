% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{build_variable_registry}
\alias{build_variable_registry}
\title{Build and write a release-specific variable registry JSON}
\usage{
build_variable_registry(release, registry_dir = NULL, verbose = TRUE)
}
\arguments{
\item{release}{Character scalar release identifier.}

\item{registry_dir}{Character scalar directory where \verb{<release>.json} is
written. Defaults to \code{.piptm_env$registry_dir}.}

\item{verbose}{Logical; passed to \code{pipload::pip_read()}.}
}
\value{
Invisibly returns the in-memory registry list.
}
\description{
Merges the UI YAML spec (\code{tm_variable_spec.yaml}) with the pipdata recode
spec to produce a release-specific registry. Only variables listed in the
\code{piptm.optional_vars} R option are admitted as optional variables.
Special-case variables (\code{welfare}, \code{pov_status}, \code{weight}) are always
included regardless of the option.
}
\details{
When \code{piptm.optional_vars} is \code{NULL}, the registry will contain only the
special-case variables and a warning is emitted. Variables in the option
that have no YAML entry are warned about and skipped. Variables in the YAML
that are not in the option are silently excluded (informed, not warned).
}
