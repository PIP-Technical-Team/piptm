% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{.load_manifests}
\alias{.load_manifests}
\title{Scan a manifest directory, parse all manifest JSON files, and cache results}
\usage{
.load_manifests(manifest_dir)
}
\arguments{
\item{manifest_dir}{Absolute path to the manifest directory.}
}
\value{
Invisibly returns the number of manifests loaded.
}
\description{
Looks for files matching the glob \code{manifest_*.json} in \code{manifest_dir},
parses each with \code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}}, converts the \code{entries} array to a
\code{data.table}, and stores all manifests in \code{.piptm_env$manifests} keyed by
\code{release} ID. Also reads \code{current_release.json} if present to set the
default release.
}
\details{
The \code{dimensions} field in each entry is stored as a list column — each
element is a character vector of available breakdown dimension names.
}
\keyword{internal}
