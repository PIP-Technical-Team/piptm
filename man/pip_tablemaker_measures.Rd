% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measures.R
\name{pip_tablemaker_measures}
\alias{pip_tablemaker_measures}
\title{Full catalogue of analysis variables for the Table Maker Step 2 UI}
\usage{
pip_tablemaker_measures()
}
\value{
A list of 12 entries in canonical display order
(welfare \eqn{\to} poverty \eqn{\to} inequality \eqn{\to} age
\eqn{\to} education binaries \eqn{\to} infrastructure binaries
\eqn{\to} labour binary).
Each entry is a named list with five fields:
\describe{
\item{\code{varname}}{Character scalar: identifier used by the backend.}
\item{\code{label}}{Character scalar: human-readable name shown in the UI
dropdown.}
\item{\code{type}}{Character scalar: one of \code{"welfare"}, \code{"poverty"},
\code{"inequality"}, \code{"continuous"}, \code{"binary"}.}
\item{\code{poverty_line_slider}}{Logical scalar: \code{TRUE} only for the
\code{"poverty"} type; tells the UI whether to render the poverty line
slider.}
\item{\code{available_stats}}{List of named lists, each with:
\code{measure} — character scalar backend measure identifier — and
\code{label} — character scalar human-readable label for the UI.}
}
}
\description{
Returns the static catalogue of all analysis variables available in the
Table Maker UI Step 2 (analysis variable dropdown).  For each variable,
the catalogue encodes its type, human-readable label, available statistics,
and whether the poverty line slider should be rendered.

The catalogue is fully static and always returns the complete universe of
analysis variables regardless of which surveys were selected in Step 1.
Whether a specific variable is computable for a given survey is handled by
the backend at query time.
}
\details{
Analysis variable types:

\describe{
\item{\code{"welfare"}}{The welfare aggregate; computed via welfare family
statistics (mean, median, distributional quantiles, etc.)}
\item{\code{"poverty"}}{Poverty status relative to a poverty line; requires
the poverty line slider.}
\item{\code{"inequality"}}{Inequality of the welfare distribution (Gini, MLD).}
\item{\code{"continuous"}}{A continuous variable (currently: age); uses the
same statistics as the welfare type.}
\item{\code{"binary"}}{A binary indicator derived from a source column (e.g.
primary education completion from \code{educat7}, infrastructure access from
dedicated columns, employment from \code{lstatus}).  Available statistics are
population share and observation share only.}
}

\code{pop_share} and \code{obs_share} appear exclusively in the \code{binary} type stats
list.  They are not meaningful in a welfare or continuous variable context.

The values in this catalogue have been verified against the Colombia 2010
survey data (COL_2010) and the GLD harmonisation codebook.  Re-verify
against those sources if changes are needed.
}
\examples{
m <- pip_tablemaker_measures()
length(m)                              # 12 analysis variables
m[[1L]]$varname                        # "welfare"
m[[2L]]$poverty_line_slider            # TRUE  (poverty type only)
m[[1L]]$available_stats[[1L]]$measure  # "mean"

}
\seealso{
Other measures: 
\code{\link{pip_age_bins}()},
\code{\link{pip_tablemaker_categories}()},
\code{\link{pip_valid_dimensions}()}
}
\concept{measures}
