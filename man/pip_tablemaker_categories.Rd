% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categories.R
\name{pip_tablemaker_categories}
\alias{pip_tablemaker_categories}
\title{Full catalogue of categorical variables for the Table Maker sample-base
filter panel}
\usage{
pip_tablemaker_categories()
}
\value{
A list of 24 entries in canonical display order
(demographic → education → household → infrastructure → labour).
Each entry is a named list with three fields:
\describe{
\item{\code{varname}}{Character scalar: column name as it exists at query
time (derived name for binned variables, raw column name for all
others).}
\item{\code{label}}{Character scalar: human-readable variable name for the
UI.}
\item{\code{subcategories}}{List of named lists, each with:
\code{value} — the value as it appears in the data (character for factors
and bins; integer for coded variables) — and \code{label} — the
human-readable subcategory label for the UI.}
}
}
\description{
Returns the static catalogue of all categorical variables available in the
PIP microdata that the Table Maker UI uses to build its sample-base filter
panel (Step 2, first decision: "What sample base do I want to analyse?").

The UI renders each variable as a filter with toggleable subcategory chips,
all selected by default.  The user can deselect subcategories to restrict
the sample base; multiple active filters are combined as intersections at
query time.  The catalogue is fully static and always returns the complete
universe of variables regardless of which surveys were selected in Step 1.
Whether a specific variable is present in a given survey is handled by the
backend at query time, not by this endpoint.
}
\details{
Variables belong to one of four tracks:
\itemize{
\item \strong{Track 1 — Factor variables} (\code{gender}, \code{area}, \code{educat4}, \code{educat5},
\code{educat7}): stored as factors in the Parquet files; subcategory \code{value}
and \code{label} are both the human-readable factor level string.
\item \strong{Track 2 — Statically binned variables} (\code{age_group}, \code{hsize_group}):
derived at query time from continuous integer columns (via \code{.bin_age()}
and \code{.bin_hsize()} respectively); only the derived column name is exposed.
\item \strong{Track 3 — Binary integer indicators} (\code{imp_wat_rec}, \code{imp_san_rec},
\code{electricity}): stored as \code{0}/\code{1} integers; subcategory \code{value} is an
integer and \code{label} is \code{"No"} / \code{"Yes"}.
\item \strong{Track 4 — Integer-coded GMD labour variables} (\code{lstatus} family,
\code{empstat} family, \code{industrycat4} family, \code{industrycat10} family):
standardised GMD codebook integers; subcategory \code{value} is an integer.
All variants within a family (\verb{_2}, \verb{_year}, \verb{_2_year}) share the same
subcategory list.
}
}
\examples{
cats <- pip_tablemaker_categories()
length(cats)                         # 24 variables
cats[[1L]]$varname                   # "gender"
cats[[1L]]$subcategories[[1L]]$value # "male"

}
\seealso{
Other measures: 
\code{\link{pip_age_bins}()},
\code{\link{pip_tablemaker_measures}()},
\code{\link{pip_valid_dimensions}()}
}
\concept{measures}
