% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_maker.R
\name{table_maker}
\alias{table_maker}
\title{Compute cross-tabulated welfare, inequality, and poverty measures}
\usage{
table_maker(
  pip_id = NULL,
  analysis_var,
  measures,
  poverty_line = NULL,
  by = NULL,
  filter_base = NULL,
  ppp = 2021L,
  release = NULL,
  pop_share_threshold = 0.01,
  include_metadata = FALSE
)
}
\arguments{
\item{pip_id}{Character vector of survey identifiers (manifest primary key,
e.g. \code{"COL_2010_GEIH_INC_ALL"}). Must be provided and non-empty.}

\item{analysis_var}{Character scalar analysis variable name.}

\item{measures}{Non-empty character vector of measure names drawn from
the internal measure registry (validated by \code{\link[=.classify_measures]{.classify_measures()}}).}

\item{poverty_line}{Positive numeric scalar poverty line, or \code{NULL}.
Required when any poverty-family measure is requested or when
\code{"pov_status"} is included in \code{by}.}

\item{by}{Character vector of disaggregation dimension names, or \code{NULL} for
aggregate results. Values must be valid covariates from
\code{\link[=piptm_layout_covariates]{piptm_layout_covariates()}} for the selected release. At most 4
dimensions are allowed.}

\item{filter_base}{Named list of sample-base filters, or \code{NULL}.
Each name is a variable and each value is an integer vector of allowed
codes (AND across variables, IN within variable). Example:
\code{list(age_group = c(1L, 2L), gender = 0L)}.}

\item{ppp}{Integer scalar PPP year (e.g. \code{2017L}). Defaults to
\code{2021L}, the current PPP baseline. Passed through to \code{\link[=load_surveys]{load_surveys()}} and
selects which \verb{welfare_ppp_*} column to use as \code{welfare} for deflated-data
surveys. Set to \code{NULL} to fall back to the manifest \code{ppp_sort} default.}

\item{release}{Character scalar release ID (e.g. \code{"20260206"}). Defaults
to the current release as returned by \code{\link[=piptm_current_release]{piptm_current_release()}}.}

\item{pop_share_threshold}{Numeric scalar in (0, 1) or \code{NULL}.  When
non-NULL, cells whose population share falls below this threshold have all
non-share measures suppressed (dropped from output), regardless of whether
\code{"pop_share"} was requested.  If \code{"pop_share"} is not among the requested
\code{measures}, it is computed internally and used solely for threshold
evaluation — it does not appear in the output.  Share-family measures
(\code{pop_share}, \code{target_within_group_share}, \code{target_survey_share}) are
always retained so callers can inspect the distribution of suppressed
groups.  Has no effect when \code{by = NULL} (aggregate mode: each cell is a
full survey with pop_share = 1.0).  Default: \code{0.01} (1\%).}

\item{include_metadata}{Logical scalar (default \code{FALSE}). When \code{FALSE}, returns
a \code{\link[data.table:data.table]{data.table::data.table()}} directly (default behavior). When \code{TRUE}, returns
a list with two elements: \code{data} (the data.table) and \code{description_metadata}
(a structured list containing execution logs, provenance, and resolved labels
for use by the \verb{/description} endpoint).}
}
\value{
When \code{include_metadata = FALSE} (default): A \code{\link[data.table:data.table]{data.table::data.table()}}
in \strong{long format} with columns:
\describe{
\item{\code{pip_id}}{Survey identifier.}
\item{\code{country_code}}{ISO3 country code.}
\item{\code{surveyid_year}}{Survey year.}
\item{\code{welfare_type}}{\code{"INC"} or \code{"CON"}.}
\item{\verb{[by cols]}}{One column per element of \code{by} (if non-NULL).
Surveys missing any requested dimension are excluded before loading;
every row in the result is guaranteed to have non-\code{NA} values for all
breakdown columns.}
\item{\code{poverty_line}}{Poverty threshold; populated for poverty-family
rows and \code{NA} for non-poverty rows.}
\item{\code{measure}}{Measure name (e.g. \code{"headcount"}, \code{"gini"}).}
\item{\code{value}}{Computed statistic.}
\item{\code{population}}{Total weighted population in the group.  For surveys
where a requested dimension is absent (partial-match), this reflects the
weighted count of respondents in the non-\code{NA} grouping cells only; rows
with \code{NA} in the missing dimension still carry the correct weighted count
for their observed group.}
}
When \code{include_metadata = TRUE}: A list with elements:
\describe{
\item{\code{data}}{The \code{\link[data.table:data.table]{data.table::data.table()}} described above.}
\item{\code{description_metadata}}{A named list containing:
\describe{
\item{\code{params}}{Echo of all function arguments.}
\item{\code{provenance}}{Release ID, PPP year, and generation timestamp.}
\item{\code{surveys}}{Data tables of loaded and excluded surveys.}
\item{\code{resolved_labels}}{UI labels for analysis_var, measures, filters, and covariates.}
\item{\code{execution}}{Execution logs including warnings and suppression events.}
}
}
}
}
\description{
Top-level API function that orchestrates the full pipeline:
manifest lookup → survey loading → pre-processing → per-survey computation
→ long-format output.
}
\details{
Requests are survey-first: callers provide one or more \code{pip_id} values plus
an explicit \code{analysis_var} that determines routing.

\code{pov_status} is treated as a derived variable (never loaded from Parquet).
When \code{"pov_status"} appears in \code{by}, it is computed as
\code{as.integer(welfare < poverty_line)} after survey loading.
}
\examples{
\dontrun{
set_manifest_dir("//server/manifests")
set_arrow_root("//server/pip/arrow")

# Welfare analysis
result <- table_maker(
  pip_id        = "COL_2010_GEIH_INC_ALL",
  analysis_var  = "welfare",
  measures      = c("headcount", "gini", "mean"),
  poverty_line  = 2.15,
  by            = c("gender", "area")
)

# Poverty-status analysis
result <- table_maker(
  pip_id        = "COL_2010_GEIH_INC_ALL",
  analysis_var  = "pov_status",
  measures      = c("headcount", "poverty_gap"),
  poverty_line  = 2.15
)
}
}
\seealso{
Other api: 
\code{\link{pip_lookup}()},
\code{\link{run_api}()}
}
\concept{api}
