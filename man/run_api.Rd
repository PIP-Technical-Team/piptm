% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{run_api}
\alias{run_api}
\title{Start the piptm Plumber API server}
\usage{
run_api(port = 8080L, host = "0.0.0.0", quiet = FALSE)
}
\arguments{
\item{port}{Integer. TCP port to listen on. Defaults to \code{8080}. Override
at deployment time via the \code{PIPTM_API_PORT} environment variable:
\code{port = as.integer(Sys.getenv("PIPTM_API_PORT", unset = "8080"))}.}

\item{host}{Character scalar. IP address to bind to. Defaults to
\code{"0.0.0.0"} (all interfaces). Use \code{"127.0.0.1"} to restrict to
localhost only.}

\item{quiet}{Logical. When \code{TRUE}, suppresses the plumber startup banner.
Defaults to \code{FALSE}.}
}
\value{
Called for its side effect (blocking HTTP server). Returns
invisibly when the server shuts down.
}
\description{
Loads the Plumber router from \code{inst/plumber/plumber.R} and starts
listening for HTTP requests.  This is a blocking call — the function
does not return until the server is stopped (Ctrl+C / SIGINT).
}
\details{
The router exposes eight endpoints:\tabular{lll}{
   Endpoint \tab Method \tab Description \cr
   \verb{/table} \tab GET, POST \tab Compute poverty / inequality / welfare measures \cr
   \verb{/lookup} \tab GET \tab Resolve country-year-welfare triplets to pip_ids \cr
   \verb{/surveys} \tab GET \tab Full manifest rows for a release \cr
   \verb{/releases} \tab GET \tab List all loaded release IDs and current release \cr
   \verb{/measures} \tab GET \tab List measure names and computation families \cr
   \verb{/dimensions} \tab GET \tab List valid disaggregation dimension names \cr
   \verb{/health} \tab GET \tab Server health check \cr
}


All responses follow a structured envelope:
\verb{\{ status, data, warnings, errors, meta \}}.

The server must be able to reach the Arrow repository and manifest
directory configured via \code{\link[=set_arrow_root]{set_arrow_root()}} / \code{\link[=set_manifest_dir]{set_manifest_dir()}} or the
\code{PIPTM_ARROW_ROOT} / \code{PIPTM_MANIFEST_DIR} environment variables.
}
\section{Known limitations (v1)}{

\itemize{
\item Single-threaded: concurrent requests queue behind each other. Response
time target is ≤3 seconds for 15 surveys; larger batches may exceed
this.
\item No authentication or rate limiting.
\item No multi-worker / load balancing support.
}
}

\examples{
\dontrun{
# Start on the default port (8080):
piptm::run_api()

# Start on a custom port, localhost only:
piptm::run_api(port = 9000L, host = "127.0.0.1")

# Or use the command-line launcher:
# Rscript inst/plumber/run.R
}
}
\seealso{
Other api: 
\code{\link{pip_lookup}()},
\code{\link{table_maker}()}
}
\concept{api}
