% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_inequality.R
\name{.gini_sorted}
\alias{.gini_sorted}
\title{Compute Gini coefficient from sorted welfare and weight vectors}
\usage{
.gini_sorted(welfare, weight)
}
\arguments{
\item{welfare}{Numeric vector of welfare values, \strong{sorted ascending}.}

\item{weight}{Numeric vector of weights, same length as \code{welfare}.}
}
\value{
A single numeric value in \eqn{[0, 1)}, or \code{NA_real_} when all
welfare values are zero.
}
\description{
Pure vectorised computation of the Gini coefficient using the Brown (1994)
lag-based trapezoid formula, implemented entirely with \code{collapse} C-level
functions for speed.  The input vectors \strong{must already be sorted in
ascending order} of welfare — the caller is responsible for sorting.
}
\details{
\strong{Formula} (Brown 1994 lag-based trapezoid):
\deqn{
  B = \frac{1}{W \cdot WY} \sum_{i=1}^{n}
    \Bigl( \sum_{j < i} w_j y_j + \tfrac{1}{2} w_i y_i \Bigr) w_i
  \quad\text{then}\quad \text{Gini} = 1 - 2B
}
where \eqn{W = \sum_i w_i} and \eqn{WY = \sum_i w_i y_i}.

Returns \code{NA_real_} when \eqn{WY = 0} (all welfare is zero), avoiding a
silent \code{NaN} from division by zero in the Lorenz normalisation.
}
\keyword{internal}
