% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_inequality.R
\name{compute_inequality}
\alias{compute_inequality}
\title{Compute inequality measures for a single survey}
\usage{
compute_inequality(dt, by = NULL, measures = NULL, grp = NULL)
}
\arguments{
\item{dt}{A \code{\link[data.table:data.table]{data.table::data.table()}} containing at minimum \code{welfare}
(numeric) and \code{weight} (numeric) columns, plus any columns named in \code{by}.
This must be a \strong{single-survey slice} — rows for exactly one \code{pip_id}.
The data.table is not modified; a working copy is created internally.}

\item{by}{A character vector of grouping column names present in \code{dt}
(e.g. \code{c("gender", "area")}), or \code{NULL} for the aggregate (no
disaggregation).}

\item{measures}{A character vector of inequality measure names to compute —
a subset of \code{c("gini", "mld")}.  \code{NULL} (default) computes both.}

\item{grp}{An optional pre-computed \code{\link[collapse:GRP]{collapse::GRP()}} object built on \code{dt}
grouped by \code{by}.  When provided, the internal \code{GRP()} call is skipped.
Ignored when \code{by} is \code{NULL}.}
}
\value{
A \code{\link[data.table:data.table]{data.table::data.table()}} in \strong{long format} with columns:
\describe{
\item{\verb{[by cols]}}{One column per element of \code{by} (if non-NULL).}
\item{\code{measure}}{(character) \code{"gini"} or \code{"mld"}.}
\item{\code{value}}{(numeric) The computed statistic.}
\item{\code{population}}{(numeric) Total weighted population in the group.}
}
}
\description{
Computes one or both of the supported inequality measures — the \strong{Gini
coefficient} and the \strong{Mean Log Deviation (MLD)} — for a single survey
slice, optionally disaggregated across one or more dimensions.
}
\details{
\strong{Gini} is computed via a fully vectorised data.table sort + cumulative-sum
approach (no per-group explicit loop).  The grouping GRP is used to assign
group ids; within each group the data are sorted by welfare, then the
trapezoid Lorenz formula is applied through \code{.gini_sorted()}.

\strong{MLD (Mean Log Deviation)} is the Theil L index:
\deqn{
  \text{MLD} = \frac{1}{\sum w_i} \sum w_i \log\!\left(
    \frac{\bar{y}_w}{y_i}\right)
}
where \eqn{\bar{y}_w} is the weighted mean of welfare.

\strong{Zero-welfare rule}: rows with \code{welfare == 0} contribute \strong{0} to MLD
(rather than \eqn{+\infty}).  They are counted in the population and in
the weighted mean, but their individual log-ratio is set to 0.  This is
consistent with the zero-welfare treatment in \code{compute_poverty()} for the
Watts index.
}
\examples{
library(data.table)
dt <- data.table(
  welfare = c(1, 2, 3, 4, 5),
  weight  = rep(1, 5)
)
compute_inequality(dt, measures = "gini")

}
\seealso{
Other compute: 
\code{\link{compute_measures}()},
\code{\link{compute_poverty}()},
\code{\link{compute_welfare}()}
}
\concept{compute}
