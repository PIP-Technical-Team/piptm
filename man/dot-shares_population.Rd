% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_shares.R
\name{.shares_population}
\alias{.shares_population}
\title{Compute subgroup population shares (no target variable)}
\usage{
.shares_population(dt, by = NULL, grp = NULL)
}
\arguments{
\item{dt}{A \code{\link[data.table:data.table]{data.table::data.table()}} for a single survey.}

\item{by}{Character vector of grouping column names, or \code{NULL}.}

\item{grp}{Optional pre-computed \code{\link[collapse:GRP]{collapse::GRP()}} matching \code{by}.}
}
\value{
A \code{\link[data.table:data.table]{data.table::data.table()}} in long format with columns:
\describe{
\item{\verb{[by cols]}}{Grouping columns (present only when \code{by} is provided).}
\item{\code{population}}{Weighted population of the subgroup.}
\item{\code{measure}}{Always \code{"pop_share"}.}
\item{\code{value}}{The computed share (0–1); \code{NA} if the survey total is zero.}
}
}
\description{
For each cell defined by \code{by}, computes the share of the survey's total
weighted population that falls in that cell:
}
\details{
\code{pop_share = weighted_pop_in_subgroup / total_weighted_pop_in_survey}

When \code{by = NULL} a single row is returned with \code{pop_share = 1} (the whole
survey over itself).
}
\keyword{internal}
