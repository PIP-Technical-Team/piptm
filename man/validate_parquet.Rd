% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_parquet.R
\name{validate_parquet}
\alias{validate_parquet}
\title{Validate a Parquet file or partition directory}
\usage{
validate_parquet(path, check = "schema")
}
\arguments{
\item{path}{For \code{check = "schema"} or \code{"data"}: absolute path to a
\code{.parquet} file.  For \code{check = "consistency"}: absolute path to a
partition directory (e.g.
\code{.../country_code=BOL/surveyid_year=2012/welfare_type=INC/version=v01_v04}).}

\item{check}{Character vector, one or more of \code{"schema"}, \code{"data"},
\code{"consistency"}.  Defaults to \code{"schema"}.}
}
\value{
A named list:
\describe{
\item{\code{valid}}{\code{TRUE} if no errors were found across all requested
checks; \code{FALSE} otherwise.}
\item{\code{errors}}{Character vector of hard failures.}
\item{\code{warnings}}{Character vector of soft issues.}
\item{\code{file}}{\code{path}, for traceability.}
}
When \code{check = "consistency"} the result also contains
\code{files_checked} (see \code{\link[=validate_partition_consistency]{validate_partition_consistency()}}).
}
\description{
A single-entry-point wrapper around the three lower-level validators.
Choose what to check via the \code{check} argument; results always follow the
same \code{list(valid, errors, warnings, file)} contract.
}
\details{
\tabular{lll}{
   \code{check} value \tab Delegates to \tab \code{path} type \cr
   \code{"schema"} \tab \code{\link[=validate_parquet_schema]{validate_parquet_schema()}} \tab file \cr
   \code{"data"} \tab \code{\link[=validate_parquet_data]{validate_parquet_data()}} \tab file \cr
   \code{"consistency"} \tab \code{\link[=validate_partition_consistency]{validate_partition_consistency()}} \tab directory \cr
}


When multiple file-level checks are requested (e.g.
\code{check = c("schema", "data")}), both are run and their results are merged:
\code{valid} is \code{FALSE} if \strong{either} check finds an error; \code{errors} and
\code{warnings} are concatenated.  Combining \code{"consistency"} with any other
check is not supported because \code{"consistency"} takes a directory while the
others take a file — pass \code{"consistency"} alone.
}
\examples{
\dontrun{
# Schema check only (default — fast, metadata only)
validate_parquet("path/to/KAZ_2006_HBS_CON_ALL-0.parquet")

# Data-quality check
validate_parquet("path/to/KAZ_2006_HBS_CON_ALL-0.parquet", check = "data")

# Both schema and data in one call
res <- validate_parquet(
  "path/to/KAZ_2006_HBS_CON_ALL-0.parquet",
  check = c("schema", "data")
)
res$valid
res$errors

# Cross-file consistency check for a partition directory
validate_parquet(
  "path/to/arrow/country_code=KAZ/surveyid_year=2006/welfare_type=CON/version=v01_v05",
  check = "consistency"
)
}
}
\seealso{
Other parquet-validation: 
\code{\link{validate_arrow}()},
\code{\link{validate_parquet_data}()},
\code{\link{validate_parquet_schema}()},
\code{\link{validate_partition_consistency}()}
}
\concept{parquet-validation}
