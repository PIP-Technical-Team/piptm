% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_summary_stats.R
\name{compute_summary_stats}
\alias{compute_summary_stats}
\title{Compute summary statistics for a single survey}
\usage{
compute_summary_stats(
  dt,
  by = NULL,
  measures = NULL,
  target_variable = "welfare",
  grp = NULL
)
}
\arguments{
\item{dt}{A \code{\link[data.table:data.table]{data.table::data.table()}} containing at minimum \code{weight}
(numeric) and the target variable. This must be a
\strong{single-survey slice} — rows for exactly one \code{pip_id}.
The data.table is not modified.}

\item{by}{A character vector of grouping column names present in \code{dt}
(e.g. \code{c("gender", "area")}), or \code{NULL} for the aggregate
(no disaggregation).}

\item{measures}{A character vector of summary measure names to compute —
a subset of:
\code{c("mean", "median", "sd", "var", "min", "max", "nobs", "p10", "p25", "p75", "p90", "sum")}.
\code{NULL} (default) computes all twelve measures.}

\item{target_variable}{Character scalar naming the variable on which
statistics should be computed. Defaults to \code{"welfare"}.}

\item{grp}{An optional pre-computed \code{\link[collapse:GRP]{collapse::GRP()}} object built from
the same \code{dt} and grouped by \code{by}. When provided, the internal
\code{GRP()} call is skipped. Ignored when \code{by} is \code{NULL}.}
}
\value{
A \code{\link[data.table:data.table]{data.table::data.table()}} in \strong{long format} with columns:
\describe{
\item{\verb{[by cols]}}{One column per element of \code{by} (if non-NULL).}
\item{\code{measure}}{(character) The measure name.}
\item{\code{value}}{(numeric) The computed statistic.}
\item{\code{population}}{(numeric) Total weighted population in the group,
computed as \code{fsum(weight, g = grp)}.}
}
}
\description{
Computes one or more summary measures — mean, median, standard deviation,
variance, min, max, non-missing observation count, and four weighted
percentiles (p10, p25, p75, p90) — for a single survey slice, optionally
disaggregated across one or more dimensions.
}
\details{
By default, statistics are computed on the \code{welfare} column. A different
target variable can be supplied via \code{target_variable}.

All weighted statistics use \code{weight} as weights via
the \code{w} argument of the corresponding {collapse} function. \code{min}, \code{max},
and \code{nobs} are unweighted. \code{nobs} counts non-missing observations of the
target variable.

\strong{{collapse} function mapping}:\tabular{lll}{
   Measure \tab collapse function \tab Notes \cr
   \code{mean} \tab \code{fmean(x, w = weight, g = grp)} \tab Weighted mean \cr
   \code{median} \tab \code{fmedian(x, w = weight, g = grp)} \tab Weighted median \cr
   \code{sd} \tab \code{fsd(x, w = weight, g = grp)} \tab Weighted SD (sample, \code{n-1}) \cr
   \code{var} \tab \code{fvar(x, w = weight, g = grp)} \tab Weighted variance (sample) \cr
   \code{min} \tab \code{fmin(x, g = grp)} \tab Observed minimum \cr
   \code{max} \tab \code{fmax(x, g = grp)} \tab Observed maximum \cr
   \code{nobs} \tab \code{fnobs(x, g = grp)} \tab Non-missing observation count \cr
   \code{p10} \tab \code{fnth(x, 0.10, w = weight, g = grp)} \tab Weighted 10th percentile \cr
   \code{p25} \tab \code{fnth(x, 0.25, w = weight, g = grp)} \tab Weighted 25th percentile \cr
   \code{p75} \tab \code{fnth(x, 0.75, w = weight, g = grp)} \tab Weighted 75th percentile \cr
   \code{p90} \tab \code{fnth(x, 0.90, w = weight, g = grp)} \tab Weighted 90th percentile \cr
   \code{sum} \tab \code{fsum(x, w = weight, g = grp)} \tab Weighted sum \cr
}
}
\examples{
\dontrun{
library(data.table)

dt <- data.table(
  welfare = c(1, 2, 3, 4, 5),
  weight  = rep(1, 5)
)

compute_summary_stats(
  dt,
  measures = c("mean", "median")
)
}
}
\seealso{
Other compute: 
\code{\link{compute_inequality}()},
\code{\link{compute_measures}()},
\code{\link{compute_poverty}()},
\code{\link{compute_shares}()}
}
\concept{compute}
