% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_poverty.R
\name{compute_poverty}
\alias{compute_poverty}
\title{Compute poverty measures for a single survey}
\usage{
compute_poverty(dt, poverty_lines, by = NULL, measures = NULL, grp = NULL)
}
\arguments{
\item{dt}{A \code{\link[data.table:data.table]{data.table::data.table()}} containing at minimum \code{welfare}
(numeric) and \code{weight} (numeric) columns, plus any columns named in \code{by}.
This must be a \strong{single-survey slice} — rows for exactly one \code{pip_id}.
The data.table is not modified; a working copy is created internally.}

\item{poverty_lines}{A positive numeric vector of poverty line values.
Must be non-empty.}

\item{by}{A character vector of grouping column names present in \code{dt}
(e.g. \code{c("gender", "area")}), or \code{NULL} for the aggregate (no
disaggregation).}

\item{measures}{A character vector of poverty measure names to compute —
a subset of \code{c("headcount", "poverty_gap", "severity", "watts", "pop_poverty")}.  \code{NULL} (default) computes all five.}

\item{grp}{An optional pre-computed \code{\link[collapse:GRP]{collapse::GRP()}} object built on \code{dt}
(or a working copy with the same rows in the same order) grouped by \code{by}.
When provided, the internal \code{GRP()} call is skipped — useful when the
caller already holds a grouping for the same data.  Ignored when \code{by} is
\code{NULL}.}
}
\value{
A \code{\link[data.table:data.table]{data.table::data.table()}} in \strong{long format} with columns:
\describe{
\item{\code{poverty_line}}{(numeric) The poverty threshold.}
\item{\verb{[by cols]}}{One column per element of \code{by} (if non-NULL).}
\item{\code{measure}}{(character) The measure name.}
\item{\code{value}}{(numeric) The computed statistic.}
\item{\code{population}}{(numeric) Total weighted population in the group.}
}
}
\description{
Computes one or more FGT (Foster-Greer-Thorbecke) poverty measures and the
Watts index for a single survey slice.  Measures are computed in a
\strong{per-poverty-line loop} that keeps memory at O(n) throughout: the
original dataset is never expanded.  \code{log(welfare)} and the grouping
object are both pre-computed once and reused across all poverty lines,
so requesting multiple lines costs little extra.
}
\details{
The poverty measures are:
\describe{
\item{\code{headcount}}{Share of the weighted population below the poverty
line: \eqn{H = \sum w_i \mathbf{1}[y_i < z] / \sum w_i}.}
\item{\code{poverty_gap}}{Mean normalised shortfall:
\eqn{PG = \sum w_i g_i / \sum w_i} where
\eqn{g_i = (z - y_i)/z} for poor individuals, 0 otherwise.}
\item{\code{severity}}{FGT(2) — squared-gap index:
\eqn{\sum w_i g_i^2 / \sum w_i}.}
\item{\code{watts}}{Watts index:
\eqn{W = \sum w_i \log(z / y_i) \mathbf{1}[y_i < z] / \sum w_i}.
Individuals with \eqn{y_i = 0} contribute 0 to the Watts sum (see
\strong{Zero-welfare rule} below).}
\item{\code{pop_poverty}}{Weighted count of poor individuals:
\eqn{\sum w_i \mathbf{1}[y_i < z]}.}
}

\strong{Zero-welfare rule}: individuals with \code{welfare == 0} are counted as poor
(their welfare is strictly below any positive poverty line) but contribute
\strong{0} to the Watts index (rather than \eqn{+\infty}).  The rule is
consistent with the treatment in \code{compute_inequality()} for MLD.
}
\examples{
library(data.table)
dt <- data.table(
  welfare = c(1, 2, 3, 4, 5),
  weight  = rep(1, 5)
)
compute_poverty(dt, poverty_lines = 3.5, measures = "headcount")

}
\seealso{
Other compute: 
\code{\link{compute_inequality}()},
\code{\link{compute_measures}()},
\code{\link{compute_welfare}()}
}
\concept{compute}
